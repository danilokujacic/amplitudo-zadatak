import React from "react";
import Grid from "../features/Grid";
import PageLayout from "../layouts/page";

const Courses = () => {
  return (
    <PageLayout title="Obuke">
      <Grid />
    </PageLayout>
  );
};

export default Courses;
